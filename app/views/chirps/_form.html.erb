<%= form_for @chirp do |f| %>
	<% if @chirp.errors.any? %>
		<div class="alert">
			<% @chirp.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</div>
	<% end %>
	
	<p><%= f.text_area :body %></p>
	<p><%= f.submit %></p>
<% end %>