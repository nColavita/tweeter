<h1>Listing Users</h1>
<ul>
	<% @users.each do |user| %>
		<li><%= link_to user.username, user_path(user.id) %> 
			(<%= link_to 'delete', user_path(user.id), method: :delete %>)</li>
	<% end %> 
</ul>
